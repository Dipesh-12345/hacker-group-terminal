import { TerminalPage } from "../components/terminal-page";
import { pageContent } from "../../lib/site-data";

export default function OutreachPage() {
  return <TerminalPage content={pageContent.outreach} />;
}
